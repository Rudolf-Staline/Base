# API demo

Consommation de `@basekit/api` : `createApiClient` pour la production,
`createMockClient` pour les démos/tests. Gestion `loading` / `success` / `error`.

Référence à copier-coller (non compilée par le monorepo). Version exécutable :
`apps/playground` → onglet _API_.

Voir `App.example.tsx`.
