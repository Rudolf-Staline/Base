# Reader demo

`ReaderLayout` pour une application de lecture (ex. lecture biblique) :
navigation latérale, zone de lecture lisible centrée, panneau secondaire
optionnel et mode focus.

Référence à copier-coller (non compilée par le monorepo). Version exécutable :
`apps/playground` → onglet _Reader_.

Voir `App.example.tsx`.
